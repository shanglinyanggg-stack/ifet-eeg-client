# Flexible SSVEP EEG Mindmap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a verified MindMaster `.emmx` copy whose SSVEP brain-computer headband branch contains the six approved implementation nodes and three approved flexibility/validation feature nodes.

**Architecture:** Preserve the source `.emmx` as immutable input, create a byte-identical working copy under `output/mindmap`, and edit only that copy through MindMaster so its proprietary `page.bin` layout and ZIP metadata are regenerated by the owning application. Keep literature evidence in a separate Markdown artifact, then validate the output at the filesystem, ZIP/XML, textual, application-reopen, and visual levels.

**Tech Stack:** Windows PowerShell, MindMaster 13.0.12, ZIP/XML validation through .NET, Codex computer-use automation, Markdown.

## Global Constraints

- Source file: `C:/Users/thulh/Downloads/可穿戴科研原型机规划.emmx`.
- Source SHA-256 must remain `68608C8B1A436D3280F2D6CF604EE4546139BD8C5B2959028E686B6745253CD5`.
- Modify only the branch `脑机头带，通过脑电控制外设（徐陆天泽、杨尚霖）` and add three nodes under the existing shared `头带 -> 特性` branch.
- Do not modify `（尝试）电刺激干预+eeg检测闭环头带` or any existing frontend source file.
- Treat full flexibility as flexible skin interface/interconnect/encapsulation plus low-profile rigid islands or a detachable electronics pod.
- Do not claim unmeasured accuracy, battery life, bending life, impedance pass rate, or NIRS performance.
- Output MindMaster file: `output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx`.
- Output evidence file: `output/mindmap/全柔性SSVEP脑机头带_导图补全依据_20260710.md`.

---

### Task 1: Create the evidence artifact and immutable working copy

**Files:**
- Create: `output/mindmap/全柔性SSVEP脑机头带_导图补全依据_20260710.md`
- Create by binary copy: `output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx`
- Read: `docs/superpowers/specs/2026-07-10-ssvep-flexible-eeg-mindmap-design.md`

**Interfaces:**
- Consumes: the approved design spec and the immutable source `.emmx`.
- Produces: an evidence Markdown and a byte-identical `.emmx` working copy for Task 2.

- [ ] **Step 1: Create the evidence Markdown with the approved node text, evidence/inference distinction, attachment page findings, and eight DOI-verified references**

Use `apply_patch` to create the file with these exact sections:

```markdown
# 全柔性 SSVEP 脑机头带导图补全依据

## 交付范围
- 只完善“脑机头带，通过脑电控制外设（徐陆天泽、杨尚霖）”。
- 原位扩写 6 个实现节点，并在共享“特性”分支增加 3 个节点。
- 不修改电刺激闭环分支；NIRS 只作为后续可选模态。

## 导图最终文案
- 硬件选型：O1/Oz/O2/POz 柔性/织物干电极，ADS1299-4 24位 EEG AFE，nRF52840，6轴 IMU，BLE，锂电池及可拆卸电子仓
- 原理图设计：电极保护/人体限流/RC抗混叠/REF+BIAS/阻抗检测 -> ADS1299-4 -> SPI主控 -> BLE；低噪声电源、充电与本地缓存
- layout设计：AFE靠近电极，模拟/数字/RF分区；差分与屏蔽走线、连续回流、蛇形应变释放，天线远离电极线和AFE
- 电路结构设计：柔性电极与互连 + 分布式微型刚岛 + 软封装 + 可拆卸后脑控制/电池仓，采集板与无线板模块化
- 嵌入式：250-500 Hz同步采样，0.5-40 Hz带通/50 Hz陷波，阻抗/SQI/IMU伪迹检测；FBCCA免训练、TRCA个体校准，时间戳/丢包检测/OTA
- APP：按实际刷新率标定SSVEP刺激，实时波形/频谱/阻抗/SQI与识别置信度；外设映射、二次确认、心跳/急停、数据记录与导出
- 全柔性工程边界：贴肤电极/互连/包封柔性，AFE/MCU/射频/电池采用低轮廓刚岛或可拆卸电子仓
- SSVEP电极与控制链：O1/Oz/O2/POz + 耳后REF/GND；刷新率约束的频率/相位编码 -> FBCCA/TRCA -> 指令确认
- 原型验证：皮肤阻抗/输入噪声/CMRR、弯折与汗液漂移、运动伪迹/舒适度、丢包/续航、科研级EEG对照、在线准确率/ITR/误触发

## 附件实证
- Nature Communications 2023 的 SpiralE 是耳内柔性传感器，不是无线全柔性头带；9 人离线 9 目标 SSVEP 平均准确率 95%/8 s，但 40 目标在线实验仅 1 人。
- 该论文依赖外部 NeuroScan、REF/GND、约 10 V 驱动和有线夹具；18 个视觉实验器件中 10 个需要凝胶，机械验证仅 10% 拉伸 400 次。
- 样机方案初稿是 ADPD4100 三波长 NIRS 链路，并明确包含柔性 FPC、刚性主板和刚性封装器件，不能直接称全柔性 EEG 整机。

## 工程推断与待验证项
- 采用柔性电极/互连/包封加低轮廓刚岛或可拆电子仓，是当前证据支持的可制造定义。
- SSVEP 首版优先验证 O1/Oz/O2/POz，纯前额方案只做对照。
- 皮肤阻抗、输入噪声、CMRR、弯折汗液漂移、在线准确率、ITR、误触发和续航必须由样机实测，不写成已实现指标。

## 近三年核心文献
1. Wang et al. (2023), *Conformal in-ear bioelectronics for visual and auditory brain-computer interfaces*. https://doi.org/10.1038/s41467-023-39814-6
2. Kaveh et al. (2024), *Wireless ear EEG to monitor drowsiness*. https://doi.org/10.1038/s41467-024-48682-7
3. Kwon et al. (2023), *At-home wireless sleep monitoring patches for the clinical assessment of sleep quality and sleep apnea*. https://doi.org/10.1126/sciadv.adg9671
4. Lopez-Larraz et al. (2023), *A garment that measures brain activity: proof of concept of an EEG sensor layer fully implemented with smart textiles*. https://doi.org/10.3389/fnhum.2023.1135153
5. Paul et al. (2023), *A Versatile In-Ear Biosensing System and Body-Area Network for Unobtrusive Continuous Health Monitoring*. https://doi.org/10.1109/TBCAS.2023.3272649
6. Mascia et al. (2023), *Wearable System Based on Ultra-Thin Parylene C Tattoo Electrodes for EEG Recording*. https://doi.org/10.3390/s23020766
7. Hou et al. (2023), *Flexible Gel-Free Multi-Modal Wireless Sensors With Edge Deep Learning for Detecting and Alerting Freezing of Gait Symptom*. https://doi.org/10.1109/TBCAS.2023.3281596
8. Mai et al. (2023), *Real-Time On-Chip Machine-Learning-Based Wearable Behind-The-Ear Electroencephalogram Device for Emotion Recognition*. https://doi.org/10.1109/ACCESS.2023.3276244
```

- [ ] **Step 2: Create the output directory and binary working copy**

Run:

```powershell
New-Item -ItemType Directory -Force -Path 'output/mindmap' | Out-Null
Copy-Item -LiteralPath 'C:/Users/thulh/Downloads/可穿戴科研原型机规划.emmx' -Destination 'output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx'
```

- [ ] **Step 3: Verify source and copy are initially byte-identical**

Run:

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath 'C:/Users/thulh/Downloads/可穿戴科研原型机规划.emmx','output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx'
```

Expected before GUI editing: both hashes equal `68608C8B1A436D3280F2D6CF604EE4546139BD8C5B2959028E686B6745253CD5`.

### Task 2: Apply the approved content through MindMaster

**Files:**
- Modify through MindMaster: `output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx`
- Read: `output/mindmap/全柔性SSVEP脑机头带_导图补全依据_20260710.md`

**Interfaces:**
- Consumes: the byte-identical working copy and the nine approved node strings.
- Produces: a MindMaster-saved `.emmx` whose proprietary layout cache and ZIP CRC are regenerated by the application.

- [ ] **Step 1: Open the working copy in MindMaster and navigate to the target branch**

Open the exact output path, expand `头带 -> 原型机`, and visually locate `脑机头带，通过脑电控制外设（徐陆天泽、杨尚霖）`. Confirm the adjacent electrical-stimulation branch is visible but remains unselected.

- [ ] **Step 2: Replace the six existing child titles in place**

Enter these exact strings, one per existing child node:

```text
硬件选型：O1/Oz/O2/POz 柔性/织物干电极，ADS1299-4 24位 EEG AFE，nRF52840，6轴 IMU，BLE，锂电池及可拆卸电子仓
原理图设计：电极保护/人体限流/RC抗混叠/REF+BIAS/阻抗检测 -> ADS1299-4 -> SPI主控 -> BLE；低噪声电源、充电与本地缓存
layout设计：AFE靠近电极，模拟/数字/RF分区；差分与屏蔽走线、连续回流、蛇形应变释放，天线远离电极线和AFE
电路结构设计：柔性电极与互连 + 分布式微型刚岛 + 软封装 + 可拆卸后脑控制/电池仓，采集板与无线板模块化
嵌入式：250-500 Hz同步采样，0.5-40 Hz带通/50 Hz陷波，阻抗/SQI/IMU伪迹检测；FBCCA免训练、TRCA个体校准，时间戳/丢包检测/OTA
APP：按实际刷新率标定SSVEP刺激，实时波形/频谱/阻抗/SQI与识别置信度；外设映射、二次确认、心跳/急停、数据记录与导出
```

- [ ] **Step 3: Add three child nodes under the existing shared `头带 -> 特性` node**

Append these exact sibling nodes after the existing feature items:

```text
全柔性工程边界：贴肤电极/互连/包封柔性，AFE/MCU/射频/电池采用低轮廓刚岛或可拆卸电子仓
SSVEP电极与控制链：O1/Oz/O2/POz + 耳后REF/GND；刷新率约束的频率/相位编码 -> FBCCA/TRCA -> 指令确认
原型验证：皮肤阻抗/输入噪声/CMRR、弯折与汗液漂移、运动伪迹/舒适度、丢包/续航、科研级EEG对照、在线准确率/ITR/误触发
```

- [ ] **Step 4: Reflow and inspect the edited region**

Use MindMaster automatic layout or local branch spacing controls. Preserve the existing green branches and theme. Confirm all text wraps inside its node, no node overlaps another, and the new feature nodes appear at the end of the existing feature branch.

- [ ] **Step 5: Save, close, and reopen the same output file**

Save with `Ctrl+S`, close the document, and reopen the exact output path. Expected: no corruption, repair, conversion, or missing-font dialog.

### Task 3: Validate the binary artifact and visual result

**Files:**
- Verify: `output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx`
- Verify: `output/mindmap/全柔性SSVEP脑机头带_导图补全依据_20260710.md`
- Create: `output/mindmap/全柔性SSVEP脑机头带_导图验收截图_20260710.png`

**Interfaces:**
- Consumes: the MindMaster-saved output from Task 2.
- Produces: validation evidence that the source is unchanged, the output container is readable, all nine strings exist once, excluded content is unchanged, and the visible layout is legible.

- [ ] **Step 1: Verify the immutable source hash and changed output hash**

Run:

```powershell
$source = Get-FileHash -Algorithm SHA256 -LiteralPath 'C:/Users/thulh/Downloads/可穿戴科研原型机规划.emmx'
$output = Get-FileHash -Algorithm SHA256 -LiteralPath 'output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx'
$source
$output
if ($source.Hash -ne '68608C8B1A436D3280F2D6CF604EE4546139BD8C5B2959028E686B6745253CD5') { throw 'Source file changed' }
if ($source.Hash -eq $output.Hash) { throw 'Output was not edited' }
```

Expected: source hash remains fixed and output hash differs.

- [ ] **Step 2: Verify every ZIP entry and XML document**

Run:

```powershell
$path = (Resolve-Path 'output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx').Path
Add-Type -AssemblyName System.IO.Compression.FileSystem
$stream = [System.IO.File]::OpenRead($path)
$zip = [System.IO.Compression.ZipArchive]::new($stream, [System.IO.Compression.ZipArchiveMode]::Read)
try {
    $required = @('document.xml','theme.xml','rels/page_formats.xml','rels/page_rels.xml','mmpage/page.bin')
    foreach ($name in $required) {
        if (-not $zip.GetEntry($name)) { throw "Missing ZIP entry: $name" }
    }
    foreach ($entry in $zip.Entries) {
        $entryStream = $entry.Open()
        try {
            $buffer = New-Object byte[] 8192
            while ($entryStream.Read($buffer, 0, $buffer.Length) -gt 0) {}
        } finally { $entryStream.Dispose() }
    }
    foreach ($name in @('document.xml','theme.xml','rels/page_formats.xml','rels/page_rels.xml')) {
        $entryStream = $zip.GetEntry($name).Open()
        $reader = [System.IO.StreamReader]::new($entryStream, [System.Text.Encoding]::UTF8)
        try { [xml]$xml = $reader.ReadToEnd() } finally { $reader.Dispose(); $entryStream.Dispose() }
    }
    $docStream = $zip.GetEntry('document.xml').Open()
    $docReader = [System.IO.StreamReader]::new($docStream, [System.Text.Encoding]::UTF8)
    try { $documentText = $docReader.ReadToEnd() } finally { $docReader.Dispose(); $docStream.Dispose() }
    if ($documentText -notmatch '<Pages V="page"') { throw 'document.xml page relationship changed' }
    $formatStream = $zip.GetEntry('rels/page_formats.xml').Open()
    $formatReader = [System.IO.StreamReader]::new($formatStream, [System.Text.Encoding]::UTF8)
    try { $formatText = $formatReader.ReadToEnd() } finally { $formatReader.Dispose(); $formatStream.Dispose() }
    if ($formatText -notmatch '<Page ID="300000100"') { throw 'Page ID changed' }
} finally { $zip.Dispose(); $stream.Dispose() }
'ZIP_XML_OK'
```

Expected: `ZIP_XML_OK` and zero stream or XML errors.

- [ ] **Step 3: Verify textual content in `mmpage/page.bin`**

Extract `mmpage/page.bin` to a temporary path and scan its UTF-8 payload. Assert each of the six implementation prefixes and three feature prefixes occurs exactly once:

```text
硬件选型：O1/Oz/O2/POz
原理图设计：电极保护
layout设计：AFE靠近电极
电路结构设计：柔性电极与互连
嵌入式：250-500 Hz同步采样
APP：按实际刷新率标定SSVEP刺激
全柔性工程边界：贴肤电极
SSVEP电极与控制链：O1/Oz/O2/POz
原型验证：皮肤阻抗
```

Also assert the exact electrical-stimulation branch title still exists and its original six placeholder child titles remain unchanged.

Run:

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
function Read-PageText([string]$emmxPath) {
    $stream = [System.IO.File]::OpenRead((Resolve-Path $emmxPath).Path)
    $zip = [System.IO.Compression.ZipArchive]::new($stream, [System.IO.Compression.ZipArchiveMode]::Read)
    try {
        $entryStream = $zip.GetEntry('mmpage/page.bin').Open()
        $memory = [System.IO.MemoryStream]::new()
        try { $entryStream.CopyTo($memory); return [System.Text.Encoding]::UTF8.GetString($memory.ToArray()) }
        finally { $memory.Dispose(); $entryStream.Dispose() }
    } finally { $zip.Dispose(); $stream.Dispose() }
}
$sourceText = Read-PageText 'C:/Users/thulh/Downloads/可穿戴科研原型机规划.emmx'
$outputText = Read-PageText 'output/mindmap/可穿戴科研原型机规划_全柔性SSVEP脑机头带完善版_20260710.emmx'
$newPrefixes = @(
    '硬件选型：O1/Oz/O2/POz',
    '原理图设计：电极保护',
    'layout设计：AFE靠近电极',
    '电路结构设计：柔性电极与互连',
    '嵌入式：250-500 Hz同步采样',
    'APP：按实际刷新率标定SSVEP刺激',
    '全柔性工程边界：贴肤电极',
    'SSVEP电极与控制链：O1/Oz/O2/POz',
    '原型验证：皮肤阻抗'
)
foreach ($prefix in $newPrefixes) {
    $count = ([regex]::Matches($outputText, [regex]::Escape($prefix))).Count
    if ($count -ne 1) { throw "Expected one occurrence of '$prefix', got $count" }
}
$oldTitles = @('硬件选型','原理图设计','layout设计','电路结构设计','嵌入式','APP')
foreach ($title in $oldTitles) {
    $pattern = [regex]::Escape($title) + '(?!：)'
    $before = ([regex]::Matches($sourceText, $pattern)).Count
    $after = ([regex]::Matches($outputText, $pattern)).Count
    if ($after -ne ($before - 1)) { throw "Unexpected placeholder count change for '$title': $before -> $after" }
}
$excluded = '（尝试）电刺激干预+eeg检测闭环头带（王宙恒、常子硕、吴桐昊）'
if (([regex]::Matches($outputText, [regex]::Escape($excluded))).Count -ne 1) { throw 'Excluded branch title changed' }
'PAGE_TEXT_OK'
```

Expected: `PAGE_TEXT_OK`.

- [ ] **Step 4: Capture and inspect the edited MindMaster region**

Fit the target implementation branch and shared feature branch in the MindMaster viewport, capture `output/mindmap/全柔性SSVEP脑机头带_导图验收截图_20260710.png`, and inspect it at original resolution. Expected: legible text, no clipping or overlap, no crossed connectors, and no accidental edits to neighboring branches.

- [ ] **Step 5: Run final artifact checklist**

Confirm both deliverables exist and are non-empty, source hash is unchanged, output hash differs, ZIP/XML validation returns zero errors, nine target strings each occur once, MindMaster reopens without repair, and the screenshot visually passes.
